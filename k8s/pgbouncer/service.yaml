apiVersion: v1

kind: Service

metadata:
  name: pgbouncer
  namespace: backend

spec:
  selector:
    app: pgbouncer
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
  type: ClusterIP